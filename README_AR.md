# 🛍️ محسّن أسعار سلة - دليل التشغيل السريع

## 📋 المتطلبات

1. **Docker Desktop** - [تحميل من هنا](https://www.docker.com/products/docker-desktop)
2. **مفاتيح API المطلوبة:**
   - OpenAI API Key
   - Tavily API Key
   - Salla Access Token
   - Salla Refresh Token

---

## 🚀 التشغيل السريع

### الخطوة 1: تشغيل Docker Desktop
افتح برنامج Docker Desktop وانتظر حتى يصبح جاهزاً

### الخطوة 2: تشغيل النظام
انقر نقراً مزدوجاً على: **`START_DOCKER.bat`**

### الخطوة 3: عرض الحاويات
1. افتح **Docker Desktop**
2. اضغط على **"Containers"** في القائمة اليسرى
3. ستجد مجموعة **"salla-price-optimizer"** مع 7 حاويات

---

## 🔧 أدوات استكشاف الأخطاء

### 1. تشخيص شامل
```bash
diagnose.bat
```
يفحص:
- حالة Docker
- تعارضات المنافذ
- ملف .env
- حالة الحاويات
- السجلات

### 2. عرض السجلات
```bash
check_logs.bat
```
يعرض سجلات جميع الخدمات أو خدمة محددة

### 3. إعادة البناء الكاملة
```bash
rebuild.bat
```
يحذف كل شيء ويعيد البناء من الصفر

### 4. إصلاح نهايات الأسطر
```bash
fix_line_endings.bat
```
يحول ملف entrypoint.sh من Windows إلى Unix

### 5. إيقاف النظام
```bash
STOP_DOCKER.bat
```
خيارات متعددة لإيقاف الحاويات

---

## 🌐 الوصول للخدمات

| الخدمة | الرابط | الوصف |
|--------|--------|-------|
| 🛍️ لوحة التحكم | http://localhost:8501 | واجهة SaaS الرئيسية |
| 🔐 API | http://localhost:8000 | OAuth و REST API |
| 🌸 Flower | http://localhost:5555 | مراقبة المهام |
| 📊 API Docs | http://localhost:8000/docs | توثيق API |

---

## 📦 الحاويات السبعة

1. **salla-postgres** - قاعدة البيانات (PostgreSQL)
2. **salla-redis** - ذاكرة التخزين المؤقت (Redis)
3. **salla-api** - خادم API (FastAPI)
4. **salla-celery-worker** - معالج المهام الخلفية
5. **salla-celery-beat** - جدولة المهام
6. **salla-flower** - مراقبة Celery
7. **salla-dashboard** - لوحة التحكم (Streamlit)

---

## ❌ المشاكل الشائعة والحلول

### المشكلة: الحاويات لا تظهر في Docker Desktop

**الحل:**
```bash
# 1. تشغيل التشخيص
diagnose.bat

# 2. عرض السجلات
check_logs.bat

# 3. إعادة البناء
rebuild.bat
```

### المشكلة: خطأ "Port already in use"

**الحل:**
```bash
# فحص المنفذ المستخدم
netstat -ano | findstr ":8000"

# إيقاف العملية
taskkill /PID <رقم_العملية> /F
```

### المشكلة: قاعدة البيانات لا تعمل

**الحل:**
```bash
# حذف البيانات وإعادة التشغيل
docker-compose down -v
docker-compose up -d
```

### المشكلة: "Permission denied: entrypoint.sh"

**الحل:**
```bash
fix_line_endings.bat
rebuild.bat
```

---

## 📝 الأوامر المفيدة

```bash
# عرض حالة الحاويات
docker-compose ps

# عرض السجلات المباشرة
docker-compose logs -f

# إعادة تشغيل خدمة معينة
docker-compose restart dashboard

# إيقاف كل شيء
docker-compose down

# إيقاف وحذف البيانات
docker-compose down -v

# إعادة بناء صورة معينة
docker-compose build --no-cache web
```

---

## 🔍 التحقق من الصحة

### فحص API
```bash
curl http://localhost:8000/health
```

### فحص قاعدة البيانات
```bash
docker-compose exec db pg_isready -U salla_user
```

### فحص Redis
```bash
docker-compose exec redis redis-cli ping
```

---

## 📚 الملفات المهمة

| الملف | الوصف |
|-------|-------|
| `START_DOCKER.bat` | تشغيل النظام |
| `STOP_DOCKER.bat` | إيقاف النظام |
| `diagnose.bat` | تشخيص المشاكل |
| `check_logs.bat` | عرض السجلات |
| `rebuild.bat` | إعادة البناء الكاملة |
| `fix_line_endings.bat` | إصلاح نهايات الأسطر |
| `TROUBLESHOOTING.md` | دليل استكشاف الأخطاء الشامل |
| `.env` | متغيرات البيئة (مفاتيح API) |
| `docker-compose.yml` | تكوين Docker |

---

## 🎯 خطوات البدء

1. ✅ تثبيت Docker Desktop
2. ✅ تشغيل Docker Desktop
3. ✅ نسخ `.env.example` إلى `.env`
4. ✅ إضافة مفاتيح API في `.env`
5. ✅ تشغيل `START_DOCKER.bat`
6. ✅ فتح http://localhost:8501
7. ✅ البدء في استخدام النظام

---

## 🆘 الحصول على المساعدة

### إذا واجهت مشكلة:

1. **شغّل التشخيص:**
   ```bash
   diagnose.bat
   ```

2. **اعرض السجلات:**
   ```bash
   check_logs.bat
   ```

3. **اقرأ دليل استكشاف الأخطاء:**
   افتح `TROUBLESHOOTING.md`

4. **جرّب إعادة البناء:**
   ```bash
   rebuild.bat
   ```

---

## 🔄 التحديثات والصيانة

### تنظيف أسبوعي
```bash
docker system prune -f
```

### تنظيف شامل شهري
```bash
docker system prune -a --volumes
```

### تحديث الصور
```bash
docker-compose pull
docker-compose up -d
```

---

## ⚙️ الإعدادات المتقدمة

### تغيير المنافذ
عدّل `docker-compose.yml`:
```yaml
ports:
  - "8001:8000"  # بدلاً من 8000:8000
```

### تغيير كلمة مرور قاعدة البيانات
عدّل `.env`:
```
DB_PASSWORD=كلمة_مرور_جديدة
```

### تغيير عدد Workers
عدّل `docker-compose.yml`:
```yaml
command: celery -A scheduler.celery_app worker --concurrency=4
```

---

## 📊 المراقبة

### Flower (مراقبة Celery)
- الرابط: http://localhost:5555
- اسم المستخدم: `admin`
- كلمة المرور: `admin123`

### Docker Desktop
- عرض استخدام الموارد
- سجلات الحاويات
- إحصائيات الأداء

---

## 🎉 النظام جاهز!

بعد تشغيل `START_DOCKER.bat` بنجاح:

1. افتح **Docker Desktop** → **Containers**
2. ستجد 7 حاويات تعمل
3. افتح http://localhost:8501
4. ابدأ في تحسين الأسعار!

---

**ملاحظة:** جميع الأدوات والسكريبتات مصممة للعمل على Windows. إذا كنت تستخدم نظام آخر، استخدم الأوامر المباشرة من `docker-compose`.
